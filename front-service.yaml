apiVersion: v1
kind: Service
metadata:
  name: front-service
  labels:
    app: front-app
spec:
  type: NodePort
  selector:
    app: front-app
  ports:
    - name: http
      nodePort: 30000
      port: 30000
      targetPort: 30000
      protocol: TCP

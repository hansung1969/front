apiVersion: v1
kind: Service
metadata:
  name: front-service
  labels:
    app: front-app
spec:
  type: NodePort
  selector:
    app: front-app
  ports:
    - name: http
      nodePort: 30001
      port: 30001
      targetPort: 30001
      protocol: TCP
